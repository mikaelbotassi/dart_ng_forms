name: dart_ng_forms
description: Reactive Forms architecture in Flutter, heavily inspired by Angular's FormGroup/FormControl pattern, with a focus on type safety, reusability and integration with TextEditingController.
version: 0.1.0
repository: https://github.com/mikaelbotassi/dart_ng_forms
issue_tracker: https://github.com/mikaelbotassi/dart_ng_forms/issues
homepage: https://github.com/mikaelbotassi/dart_ng_forms
publish_to: https://pub.dev

topics:
  - forms
  - state-management
  - validation
  - flutter
  - reactive-forms

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  result_dart: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^3.0.0
