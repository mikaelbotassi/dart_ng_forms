class FormControlOptions {
  bool isRequired;
  bool isDisabled;
  bool isReadonly;

  FormControlOptions({
    this.isRequired = false,
    this.isDisabled = false,
    this.isReadonly = false,
  });
}
